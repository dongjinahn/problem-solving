package LeetCode

import scala.collection.mutable.ListBuffer

object LcProblem5 extends App {
  def longestPalindrome(s: String): String = {
    val buffer = new ListBuffer[Char]()

    ""
  }

  println(longestPalindrome("babad"))
  println(longestPalindrome("cbbd"))
}
